const positionListModule = {
  state: {
    showPosition: false
  },
  mutations: {
    updateShowPosition(state, showPosition) {
      state.showPosition = showPosition
    }
  }
}

export default positionListModule
